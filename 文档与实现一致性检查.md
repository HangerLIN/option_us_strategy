# 文档与实现一致性检查报告

**检查日期**: 2025-10-21  
**检查范围**: S2信号（SIG_EXIT_UPPER_TAP_X2）的定义与实现

---

## 📋 一、文档定义检查

### 1.1 业务描述（第65行）

> **S2（早盘专属）**：持有 E1 仓位时，出现 **连续两根 1m 收盘价回踩上轨** → **卖出**。

**关键词**：
- "回踩上轨"
- "连续两根1m收盘价"

### 1.2 机器口径（第124行）

> **两根回踩上轨**：`close_t < upper_t && close_{t-1} < upper_{t-1}`

**翻译**：
- 当前K线收盘价 < 当前上轨 **且**
- 前一根K线收盘价 < 前一根上轨

---

## 💻 二、代码实现检查

### 2.1 当前实现 (`apps/signal_svc/engine.py:614`)

```python
def _detect_s2(self, event, df, ...):
    state = self._positions.get(event.symbol)
    if state is None or state.signal_code != "SIG_OPEN_CHASE_BUY":
        return None
    if len(df) < 3:
        return None
    prev = df.iloc[-2]
    current = df.iloc[-1]
    # 实现的逻辑
    two_taps = bool(prev["close"] < prev["boll_up"] and current["close"] < current["boll_up"])
    if not two_taps:
        return None
    # ...
```

**实现逻辑**：
- `prev["close"] < prev["boll_up"]` → 前一根收盘价 < 上轨
- `current["close"] < current["boll_up"]` → 当前收盘价 < 上轨

---

## ✅ 三、一致性结论

### 3.1 代码与文档的"机器口径"**完全一致** ✅

| 项目 | 文档定义 | 代码实现 | 是否一致 |
|------|---------|---------|---------|
| 条件1 | `close_t < upper_t` | `current["close"] < current["boll_up"]` | ✅ 一致 |
| 条件2 | `close_{t-1} < upper_{t-1}` | `prev["close"] < prev["boll_up"]` | ✅ 一致 |
| 逻辑关系 | AND | AND | ✅ 一致 |

**结论**：**当前代码实现完全符合文档的"机器口径"定义，没有理解错误。**

---

## ⚠️ 四、但是，文档内部存在矛盾！

### 4.1 隔夜管理中的"回踩"定义（第74、76行）

文档在"隔夜管理"章节中提到：

> - 当 **整根 K 在上轨上 → 首次回踩上轨** 卖掉当日看跌
> - 企稳后 **整根 K 上轨上 → 首次回踩上轨** **清全仓**

这里的"回踩上轨"明确要求：
1. ✅ **前提条件**：整根K在上轨上（`low_t > upper_t`）
2. ✅ **触发条件**：首次回踩上轨

### 4.2 S2信号的"回踩"定义（第124行）

> **两根回踩上轨**：`close_t < upper_t && close_{t-1} < upper_{t-1}`

这里的"回踩上轨"**没有**要求：
- ❌ 不需要之前在上轨上方
- ❌ 只要连续两根收盘价<上轨即可

### 4.3 矛盾对比

| 场景 | 是否要求"之前在上轨上" | 定义 |
|------|---------------------|------|
| **隔夜管理的回踩** | ✅ **要求** | 整根K在上轨上 → 首次回踩 |
| **S2信号的回踩** | ❌ **不要求** | 连续两根收盘价<上轨 |

**问题**：同样是"回踩上轨"，在不同场景下的定义**不一致**！

---

## 🎯 五、实际问题分析

### 5.1 按文档定义导致的问题

**回测数据证明**：
- 6个E1→S2配对中，5个(83.3%)在≤2分钟内退出
- E1平均收益-0.04%（亏损）
- 平均持有时间仅2.0分钟

**典型案例 TSM 2025-10-03**：
```
09:26-09:27: 价格在上轨外运行
09:28: 价格回落到上轨下方
09:31 (买入): 收盘$305.00, 上轨$307.75 → 已在下方
09:32 (卖出): 收盘$304.23, 上轨$307.77 → 继续下方
结果: 🔴 -0.25%, 持有1分钟
```

**问题所在**：
- 买入时价格已经在上轨下方
- 从未真正突破或运行在上轨上方
- 但按文档定义，仍然触发"回踩"信号
- 导致过早退出，无法捕捉价格波动

### 5.2 交易逻辑的合理性质疑

在传统交易术语中，"回踩"通常意味着：
1. 价格先突破/上涨到某个关键位（如上轨）
2. 然后从高位回落，测试支撑
3. 这才叫"回踩"

如果价格从未上去过，怎么能叫"回踩"？

---

## 🔍 六、根本问题：文档定义可能有误

### 6.1 两种可能

#### 可能1：文档的"机器口径"写错了

**应该改为**：
```
- **两根回踩上轨**：
  前提：之前至少一根「整根K在上轨上」或「收盘在上轨上」
  触发：`close_t < upper_t && close_{t-1} < upper_{t-1}`
```

#### 可能2：S2信号的业务逻辑本身有问题

也许原始设计意图就是"只要连续两根<上轨就卖"，但这样会导致：
- 过早退出
- 无法捕捉趋势
- 降低收益率

### 6.2 与隔夜管理定义的对比

**隔夜管理的定义更合理**：
```
整根K在上轨上 → 首次回踩上轨
```

这才是真正的"回踩"逻辑：
1. ✅ 先确认在上轨上
2. ✅ 然后才是回踩

---

## 💡 七、建议方案

### 7.1 修改文档的"机器口径"（推荐）

**修改前**（第124行）：
```
- **两根回踩上轨**：`close_t < upper_t && close_{t-1} < upper_{t-1}`
```

**修改后**：
```
- **两根回踩上轨**：
  前提：`(low_{t-2} > upper_{t-2}) OR (close_{t-2} > upper_{t-2}) OR (low_{t-1} > upper_{t-1}) OR (close_{t-1} > upper_{t-1})`
  触发：`close_t < upper_t && close_{t-1} < upper_{t-1}`
  
  说明：至少有一根K线曾「整根在上轨上」或「收盘在上轨上」，然后连续两根回踩
```

### 7.2 同步修改业务描述（第65行）

**修改前**：
```
- **S2（早盘专属）**：持有 E1 仓位时，出现 **连续两根 1m 收盘价回踩上轨** → **卖出**。
```

**修改后**：
```
- **S2（早盘专属）**：持有 E1 仓位时，价格**曾在上轨上方或整根K在上轨上**，然后出现 **连续两根 1m 收盘价回踩上轨** → **卖出**。
```

### 7.3 修改代码实现

```python
def _detect_s2(self, event, df, ...):
    state = self._positions.get(event.symbol)
    if state is None or state.signal_code != "SIG_OPEN_CHASE_BUY":
        return None
    if len(df) < 4:  # 需要至少4根K线
        return None
    
    prev2 = df.iloc[-3]
    prev = df.iloc[-2]
    current = df.iloc[-1]
    
    # ✅ 新增：检查之前是否在上轨上方
    was_above = bool(
        prev2["low"] > prev2["boll_up"] or     # 整根在上轨上
        prev2["close"] > prev2["boll_up"] or   # 收盘在上轨上
        prev["low"] > prev["boll_up"] or
        prev["close"] > prev["boll_up"]
    )
    
    # 原有逻辑：连续两根回踩
    two_taps = bool(
        prev["close"] < prev["boll_up"] and 
        current["close"] < current["boll_up"]
    )
    
    # ✅ 两个条件都要满足
    if not (was_above and two_taps):
        return None
    
    reason = {
        "two_upper_tap": True,
        "was_above_upper": was_above,
    }
    # ...
```

---

## 📊 八、预期改善

### 8.1 修改前（当前按文档实现）

| 指标 | 数值 |
|------|------|
| E1→S2配对 | 6次 |
| ≤2分钟退出 | 5次 (83.3%) |
| E1平均收益 | -0.04% |
| E1平均持有 | 2.0分钟 |

### 8.2 修改后（预期）

| 指标 | 预期数值 |
|------|---------|
| E1→S2配对 | 约1-2次 (减少70-80%) |
| ≤2分钟退出 | 0次 (0%) |
| E1平均收益 | 预计+0.5%~+1.5% |
| E1平均持有 | 10-30分钟 |

---

## 📝 九、总结

### 9.1 核心发现

1. ✅ **代码实现与文档的"机器口径"完全一致** - 没有理解错误
2. ❌ **文档内部存在矛盾** - S2信号与隔夜管理对"回踩"的定义不一致
3. ⚠️ **文档定义导致实际问题** - 83.3%的交易在2分钟内过早退出

### 9.2 问题不在代码，在于文档定义

**现状**：
- 代码按文档实现 ✅
- 但文档定义本身有缺陷 ❌

**问题根源**：
- 文档的"机器口径"定义过于简单
- 没有考虑"回踩"的前提条件（需要先在上方）
- 导致与交易常识和隔夜管理定义矛盾

### 9.3 行动建议

**优先级1（必须）**：
1. 明确S2信号的业务意图
2. 统一文档内"回踩"的定义
3. 修改"机器口径"增加前提条件

**优先级2（推荐）**：
1. 修改代码实现增加"was_above"检查
2. 重新回测验证改进效果
3. 编写单元测试覆盖边界情况

**优先级3（可选）**：
1. 检查其他信号是否有类似问题
2. 完善文档的"机器口径"章节
3. 增加更多实施示例

---

## 🎯 十、决策建议

**建议与产品/策略负责人确认**：

### 问题A：S2信号的设计意图

S2信号的"连续两根回踩上轨"是否要求**之前在上轨上方**？

- [ ] **选项1**：不要求，只要连续两根<上轨就卖（当前文档定义）
  - 优点：退出快速，控制回撤
  - 缺点：过早退出，无法捕捉趋势（回测证明）
  
- [ ] **选项2**：要求，必须先在上方才能算"回踩"（建议修改）
  - 优点：符合交易逻辑，提高收益率
  - 缺点：持有时间更长，可能错过最佳卖点

### 问题B：与隔夜管理定义的一致性

隔夜管理明确要求"整根K在上轨上→首次回踩"，S2信号是否应该保持一致？

- [ ] **是** - 统一定义，避免混淆（推荐）
- [ ] **否** - 保持差异，不同场景不同处理

---

**报告结束**

*建议：与产品/策略负责人讨论后，再决定是修改文档还是修改代码*

