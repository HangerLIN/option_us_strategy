# 信号过滤深度分析：为什么买入信号这么少？

**批次ID**: `bt-20250902-20250905-a10deb`  
**分析日期**: 2025年10月30日  
**问题**: 为什么只有4个买入信号？为什么没有尾盘抄底信号？

---

## 📊 信号检测总览

### E1信号（开盘追涨）

| 指标 | 数量 | 占比 |
|------|------|------|
| **检测总数** | 40次 | 100% |
| **生成买入信号** | 4次 | **10%** |
| **被过滤** | 36次 | **90%** |

### E2信号（尾盘抄底）

| 指标 | 数量 | 占比 |
|------|------|------|
| **检测总数** | 12次 | 100% |
| **生成买入信号** | 0次 | **0%** |
| **被过滤** | 12次 | **100%** |

---

## 🚫 核心问题1：Top5数据缺失导致大量过滤

### 问题严重性：⭐⭐⭐⭐⭐

**E1信号过滤原因分布**：

| 过滤原因 | 次数 | 占比 | 严重性 |
|---------|------|------|--------|
| **top5过滤** | **30次** | **75%** | 🔥🔥🔥🔥🔥 |
| candle_pattern | 4次 | 10% | ⚠️ |
| mfi_stoch | 1次 | 2.5% | ✅ |
| mfi_data_missing | 1次 | 2.5% | ⚠️ |

### 详细分析

**9月2-4日：完全没有Top5数据**

```sql
-- 数据情况
9月2日: Top5数量 = 0
9月3日: Top5数量 = 0  
9月4日: Top5数量 = 0
9月5日: Top5数量 = 10 ✅
```

**影响**：
- 9月2-4日：所有信号（30个）都被Top5过滤拒绝
- 9月5日：只有10只股票可以检测

**计算**：
- 每个股票每天至少检测1次E1信号
- 10只股票 × 4天 = 40次检测
- 其中30次（3天×10只）被Top5过滤

### 根本原因

**Top5数据未生成**：
```sql
-- 检查pm_top5表
SELECT trade_date_et, COUNT(*) as symbol_count
FROM pm_top5
WHERE trade_date_et BETWEEN '2025-09-02' AND '2025-09-04'
GROUP BY trade_date_et;
-- 结果：0行（没有数据）
```

**可能原因**：
1. 盘前选股脚本未运行
2. 数据导入问题
3. 计算逻辑错误

---

## 🚫 核心问题2：E2技术指标条件过严

### 问题严重性：⭐⭐⭐⭐⭐

**E2信号100%被过滤**，原因是技术指标得分不足。

### 技术指标要求

E2需要满足**3个条件中至少2个**：

| 条件 | 要求 | 通过率 | 状态 |
|------|------|--------|------|
| **RVOL > 2.0** | 成交量是6日均量的2倍以上 | 17% (2/12) | ⚠️ 很难 |
| **OBV金叉** | OBV上穿OBV_EMA20 | **0% (0/12)** | ❌ 从未满足 |
| **AO > 0** | Awesome Oscillator为正 | **0% (0/12)** | ❌ 从未满足 |

### 实际检测案例

**AVGO案例（9月5日）**：

| 时刻 | RVOL | OBV金叉 | AO>0 | 得分 | 结果 |
|------|------|---------|------|------|------|
| 检测1 | 2.13 ✓ | ✗ | ✗ | 1/3 | ❌ 过滤 |
| 检测2 | 0.96 ✗ | ✗ | ✗ | 0/3 | ❌ 过滤 |
| 检测3 | 1.08 ✗ | ✗ | ✗ | 0/3 | ❌ 过滤 |
| 检测4 | 0.99 ✗ | ✗ | ✗ | 0/3 | ❌ 过滤 |

**U案例（9月5日）**：

| 时刻 | RVOL | OBV金叉 | AO>0 | 得分 |
|------|------|---------|------|------|
| 检测1 | 1.68 ✗ | ✗ | ✗ | 0/3 |

### 核心问题

1. **OBV金叉从未发生**（0/12）
   - 可能是市场环境不支持
   - 或者检测逻辑有问题

2. **AO>0从未满足**（0/12）
   - 说明所有时刻趋势都不是正向
   - E2是反弹抄底信号，在下跌趋势中检测
   - 但要求AO>0（正趋势）存在矛盾

3. **RVOL>2.0很难满足**（17%通过率）
   - 要求成交量是6日均量的2倍
   - 这个阈值可能过高

### 逻辑矛盾

**E2的设计矛盾**：
- **目的**：在盘中回调时抄底
- **条件**：AO>0（要求上升趋势）
- **矛盾**：回调时趋势通常是负的，AO<0

```
回调场景：
价格下跌 → AO<0 → 不满足E2条件 → 无法抄底
但设计初衷就是在回调时抄底！
```

---

## 🎯 详细过滤流程分析

### E1信号（开盘追涨）流程

```
40次E1检测
    │
    ├─ Top5过滤：30次 (75%)
    │   └─ 9月2-4日无Top5数据
    │
    ├─ 通过Top5：10次
    │   │
    │   ├─ K线形态过滤：4次 (10%)
    │   │   └─ ASML, ANET, AFRM, STM（横盘，不满足two_green或opens_above_mid）
    │   │
    │   ├─ MFI/Stoch过滤：1次 (2.5%)
    │   │   └─ U（MFI下降+Stoch过热）
    │   │
    │   ├─ MFI数据缺失：1次 (2.5%)
    │   │   └─ HUBS
    │   │
    │   └─ 生成买入信号：4次 (10%)
    │       └─ AVGO, TSLA, TSM, LRCX
```

### E2信号（尾盘抄底）流程

```
12次E2检测
    │
    └─ 技术指标过滤：12次 (100%)
        │
        ├─ RVOL<2.0：10次
        ├─ OBV未金叉：12次
        └─ AO≤0：12次
        
        → tech_score = 0/3 或 1/3（需要2/3）
        → 全部被过滤
```

---

## 💡 改进建议

### 优先级1：解决Top5数据缺失（🔥 紧急）

**问题**：75%的E1信号因Top5数据缺失被过滤

**解决方案**：

1. **检查pm_top5表数据**：
   ```sql
   -- 检查9月2-4日数据
   SELECT * FROM pm_top5 
   WHERE trade_date_et BETWEEN '2025-09-02' AND '2025-09-04';
   ```

2. **重新运行盘前选股**：
   ```bash
   # 为9月2-4日生成Top5数据
   python -m apps.backtest.pipeline.premarket \
     --start "2025-09-02" \
     --end "2025-09-05" \
     --batch "bt-20250902-20250905-a10deb"
   ```

3. **验证数据完整性**：
   ```sql
   -- 确保每天都有数据
   SELECT trade_date_et, COUNT(DISTINCT symbol) as symbol_count
   FROM pm_top5
   WHERE trade_date_et BETWEEN '2025-09-02' AND '2025-09-05'
   GROUP BY trade_date_et
   ORDER BY trade_date_et;
   -- 预期：每天5-10只股票
   ```

**预期效果**：
- 从40次检测中恢复30次（75%）
- 可能生成10-15个额外的买入信号

---

### 优先级2：优化E2技术指标条件（🔥 重要）

**问题**：E2信号100%被过滤，OBV金叉和AO>0从未满足

#### 方案A：放宽技术指标要求（推荐）

**当前**：需要3个条件中的2个
**建议**：需要3个条件中的1个

```python
# 当前逻辑
tech_score = sum([rvol_pass, obv_cross, ao_positive])
if tech_score < 2:  # 需要2/3
    return None

# 建议修改
tech_score = sum([rvol_pass, obv_cross, ao_positive])
if tech_score < 1:  # 只需要1/3
    return None
```

**预期效果**：
- 2次检测会通过（RVOL>2.0的情况）
- 可能生成1-2个E2买入信号

#### 方案B：调整RVOL阈值

**当前**：RVOL > 2.0（通过率17%）
**建议**：RVOL > 1.5（放宽）

```python
rvol_pass = bool(rvol6 > 1.5)  # 从2.0降低到1.5
```

**预期效果**：
- 通过率从17%提升至40-50%
- 配合方案A，可能生成3-4个E2信号

#### 方案C：移除AO>0条件（激进）

**问题**：E2是抄底信号，在回调时检测，但AO<0
**建议**：移除AO>0条件，或改为AO趋势上升（不要求>0）

```python
# 当前
ao_positive = bool(ao_prev > 0 and ao_current > ao_prev)

# 建议修改1：只要求上升趋势
ao_rising = bool(ao_current > ao_prev)

# 建议修改2：或者完全移除AO条件
# 只用RVOL和OBV两个条件，需要1/2
```

---

### 优先级3：添加E2检测日志

**问题**：当前日志不够详细，难以诊断

**建议**：添加类似E1的详细日志

```python
LOGGER.info(
    "signal.e2.enter",
    symbol=symbol,
    timestamp=et_time.isoformat(),
    reason="开始尾盘抄底检测"
)

LOGGER.info(
    "signal.e2.scenario_check",
    symbol=symbol,
    scenario=scenario,
    price_vs_boll_low=f"{current_close:.2f} vs {prev_boll_low:.2f}",
    passed=scenario_pass
)
```

---

## 📈 预期优化效果

### 如果解决Top5数据问题

| 指标 | 当前 | 优化后 | 提升 |
|------|------|--------|------|
| E1检测次数 | 40 | 40 | - |
| E1被Top5过滤 | 30 (75%) | 0 (0%) | ✅ -75% |
| E1生成信号 | 4 (10%) | 12-18 (30-45%) | ⬆️ +200-350% |

### 如果优化E2技术指标（方案A+B）

| 指标 | 当前 | 优化后 | 提升 |
|------|------|--------|------|
| E2检测次数 | 12 | 12 | - |
| E2技术指标得分≥1 | 2 (17%) | 6-8 (50-67%) | ⬆️ +200-300% |
| E2生成信号 | 0 (0%) | 3-5 (25-42%) | ⬆️ 新增 |

### 总体预期

| 指标 | 当前 | 优化后 |
|------|------|--------|
| **买入信号总数** | **4个** | **15-23个** |
| **信号多样性** | 仅E1 | E1+E2 |
| **日均信号数** | 1个 | 3.75-5.75个 |

---

## 🎯 执行计划

### 第1步：修复Top5数据（立即）

```bash
# 1. 检查数据
psql -c "SELECT trade_date_et, COUNT(*) FROM pm_top5 
         WHERE trade_date_et BETWEEN '2025-09-02' AND '2025-09-05' 
         GROUP BY trade_date_et;"

# 2. 重新生成
python -m apps.backtest.pipeline.premarket \
  --start "2025-09-02" \
  --end "2025-09-05"

# 3. 验证
psql -c "SELECT trade_date_et, COUNT(*) as cnt 
         FROM pm_top5 
         WHERE trade_date_et >= '2025-09-02' 
         GROUP BY trade_date_et;"
```

### 第2步：优化E2技术指标（本周）

```python
# apps/signal_svc/engine.py
# 在_detect_e2函数中修改

# 当前
tech_score = sum([rvol_pass, obv_cross, ao_positive])
if tech_score < 2:  # 需要2/3
    return None

# 修改为
rvol_pass = bool(rvol6 > 1.5)  # 从2.0降低到1.5
tech_score = sum([rvol_pass, obv_cross, ao_positive])
if tech_score < 1:  # 从2/3降低到1/3
    LOGGER.info(
        "signal.e2.technical_filtered",
        symbol=symbol,
        rvol_pass=rvol_pass,
        obv_cross=obv_cross,
        ao_positive=ao_positive,
        tech_score=f"{tech_score}/3",
        reason=f"E2技术指标不足：需满足1/3条件，实际{tech_score}/3"
    )
    return None
```

### 第3步：重新回测验证（本周）

```bash
# 运行优化后的回测
python -m apps.backtest.main \
  --start "2025-09-02T00:00:00-04:00" \
  --end "2025-09-06T00:00:00-04:00" \
  --track equity \
  --signal-mode recompute \
  --risk-mode inproc \
  --universe "ref_market_cap:GLOBAL"

# 对比结果
# 预期：15-23个买入信号（当前：4个）
```

---

## 📊 关键指标对比

### 当前状态 vs 优化后

| 维度 | 当前 | 优化后 | 变化 |
|------|------|--------|------|
| **E1信号数** | 4 | 12-18 | +200-350% |
| **E2信号数** | 0 | 3-5 | 新增 |
| **总信号数** | 4 | 15-23 | +275-475% |
| **数据完整性** | 25% (1/4天) | 100% (4/4天) | +300% |
| **策略多样性** | 单一 | 双策略 | ✅ |

---

## 🎯 结论

### 根本原因

1. **Top5数据缺失**（75%问题）
   - 9月2-4日完全没有Top5数据
   - 导致30个E1信号被过滤

2. **E2技术指标过严**（100%问题）
   - RVOL>2.0阈值过高（17%通过率）
   - OBV金叉从未发生（0%）
   - AO>0与抄底逻辑矛盾（0%）
   - 需要2/3条件，实际最多1/3

### 紧急行动

1. ✅ **立即**：修复Top5数据缺失
2. ✅ **本周**：优化E2技术指标（降低阈值）
3. ✅ **本周**：重新回测验证效果

### 预期效果

优化后信号数从**4个**提升至**15-23个**，提升约**4-5倍**。

---

**分析完成时间**: 2025年10月30日  
**优先级**: 🔥🔥🔥🔥🔥 紧急  
**状态**: 待执行

