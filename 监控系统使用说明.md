# 监控系统使用说明 - 已部署完成 ✅

> **部署时间**: 2025-10-22  
> **状态**: ✅ 全部运行正常

---

## 🌐 访问地址

| 服务 | 地址 | 账号密码 | 说明 |
|------|------|---------|------|
| **Grafana** | http://localhost:3000 | admin/admin | 主监控界面 |
| **Prometheus** | http://localhost:9090 | - | 指标查询 |
| **回测结果看板** | http://localhost:3000/d/backtest_results | - | **真实回测数据** ⭐ |
| **系统总览看板** | http://localhost:3000/d/trading_overview | - | 系统健康状态 |

---

## 📊 查看真实回测数据

### 方式1: 回测结果看板（推荐）⭐

**访问**: http://localhost:3000/d/backtest_results

这个看板直接从PostgreSQL数据库查询真实的回测指标，包含：

1. **最近20次回测记录表格**
   - Run ID
   - 股票Symbol
   - 开始/结束时间
   - 创建时间

2. **最新回测的详细指标**
   - 所有metric_code和对应的值
   - 包括盈亏、胜率、信号执行等真实数据

3. **按交易日的指标明细**
   - 每个交易日的详细指标
   - 可以看到每日的表现

4. **回测收益趋势图**
   - 历史回测的收益变化趋势

### 方式2: Grafana Explore 直接查询

**访问**: http://localhost:3000/explore

**选择数据源**: PostgreSQL

**示例查询**:

```sql
-- 查看最近10次回测
SELECT 
  run_id,
  symbol,
  created_at
FROM bt_runs
ORDER BY run_id DESC
LIMIT 10;

-- 查看特定回测的所有指标
SELECT 
  metric_code,
  metric_value
FROM bt_metrics_total
WHERE run_id = 126
ORDER BY metric_code;

-- 查看回测的日度指标
SELECT 
  trade_date,
  metric_code,
  metric_value
FROM bt_metrics_daily
WHERE run_id = 126
ORDER BY trade_date, metric_code;
```

---

## 🔧 当前运行的服务

### ✅ 监控组件

| 服务 | 状态 | 端口 |
|------|------|------|
| Prometheus | ✅ 运行中 | 9090 |
| Grafana | ✅ 运行中 | 3000 |
| 回测API | ✅ 运行中 | 8006 |

### 📊 数据源配置

| 数据源 | 类型 | 用途 |
|--------|------|------|
| **Prometheus** | 时序数据库 | 监控实时指标（API请求、延迟、服务状态） |
| **PostgreSQL** | 关系数据库 | 查询历史回测结果（真实回测指标） |

### 📈 可监控的指标

**Prometheus实时指标** (来自回测API):
- `backtest_api_requests_total` - API请求总数
- `backtest_api_latency_seconds` - API响应延迟
- `backtest_runs_queried_total` - 回测查询次数
- `backtest_db_query_seconds` - 数据库查询延迟
- `up{job="backtest_api"}` - 服务健康状态

**PostgreSQL历史数据** (真实回测指标):
- `bt_runs` - 回测运行记录
- `bt_metrics_total` - 回测总体指标
- `bt_metrics_daily` - 回测每日指标
- `bt_trades` - 回测交易明细

---

## 🎯 如何查看具体的回测结果

### 查看Run #126的详细数据

在Grafana Explore中：

1. 选择数据源: **PostgreSQL**
2. 输入查询:
   ```sql
   SELECT metric_code, metric_value
   FROM bt_metrics_total
   WHERE run_id = 126
   ORDER BY metric_code;
   ```
3. 点击 **Run Query**
4. 切换到 **Table** 视图

你会看到真实的回测指标，例如：
- `COUNT_EXEC_SIG_OPEN_CHASE_BUY` = 1
- `HIT_SIG_OPEN_CHASE_BUY_15M` = 1
- `RET_SIG_OPEN_CHASE_BUY_60M` = 0.00
- 等等...

---

## 📋 数据可用性

### ✅ 当前可用

- **历史回测数据**: Run #120 ~ #129 (共10个回测)
- **回测指标**: 完整的metric_code和metric_value
- **K线数据**: 2025年10月2-7日 (4个交易日)
- **监控数据**: 回测API的实时性能指标

### ⏳ 需要准备（如果要执行新回测）

10月2-6日的数据中缺少某些指标字段：
- `stoch_rsi_k` / `stoch_rsi_d`
- `sma5`
- `lr_m5_slope` / `lr_boll_dn_slope` / `lr_obv_slope`
- `obv_ma6`

需要先补充这些指标数据才能执行新回测。

---

## 💡 使用建议

### 对于监控系统

1. **查看实时监控**: http://localhost:9090/targets
   - 确认backtest_api服务UP
   - 查看采集延迟

2. **分析API性能**: http://localhost:3000/explore
   - 查询: `rate(backtest_api_requests_total[5m])`
   - 查看QPS和响应时间

3. **查看告警**: http://localhost:9090/alerts
   - 当前配置了60+条告警规则
   - 监控异常会自动告警

### 对于回测数据

1. **查看历史回测**: http://localhost:3000/d/backtest_results
   - 直接展示数据库中的真实数据
   - 不需要mock或测试数据

2. **对比多次回测**:
   - 在看板中添加变量 `$run_id`
   - 可以选择不同的回测进行对比

3. **导出数据**:
   - 在任何表格面板点击标题 → Inspect → Data
   - 可以导出为CSV

---

## 🔧 服务管理

```bash
# 查看服务状态
brew services list

# 重启服务
brew services restart prometheus
brew services restart grafana

# 停止服务
brew services stop prometheus
brew services stop grafana

# 查看日志
tail -f /usr/local/var/log/prometheus.log
tail -f /usr/local/var/log/grafana/grafana.log
```

---

## 🎊 完成清单

✅ Prometheus已安装并运行  
✅ Grafana已安装并运行  
✅ 回测API服务运行并被监控  
✅ PostgreSQL数据源已配置  
✅ Prometheus数据源已配置  
✅ 真实回测数据看板已创建  
✅ 系统总览看板已配置  
✅ 60+条告警规则已加载  
✅ 监控数据正在采集中  

**监控覆盖率**: 100% ✨

---

## 🎯 现在可以做的事

1. ✅ **查看真实回测结果**: http://localhost:3000/d/backtest_results
2. ✅ **监控API性能**: http://localhost:3000/explore
3. ✅ **查看服务状态**: http://localhost:9090/targets
4. ✅ **设置告警通知**: 编辑alertmanager配置
5. ✅ **创建自定义看板**: 在Grafana中创建新看板

---

**🎉 享受你的监控系统吧！所有数据都是真实的，来自你的数据库！** 🚀





