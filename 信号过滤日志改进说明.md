# 信号过滤日志改进说明

**修改时间**：2025-10-30  
**修改文件**：`apps/signal_svc/engine.py`  
**目的**：为所有进入Top10但未生成买入信号的股票添加详细的过滤日志

---

## 📋 改进概述

为信号检测引擎的**所有过滤层**添加了详细的结构化日志，可以清楚地追踪每只Top10股票在哪个环节被过滤掉。

---

## ✅ 新增日志点

### 1. **E1信号（SIG_OPEN_CHASE_BUY）- 开盘追涨**

#### 1.1 K线形态过滤
```python
LOGGER.info(
    "signal.e1.candle_pattern_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    two_green=two_green,
    opens_above_mid=opens_above_mid,
    prev_candle="O:xxx C:xxx",
    prev2_candle="O:xxx C:xxx",
    boll_mid_prev="xxx",
    reason="K线形态不符：既不是连续两根阳线，也不是开盘在布林中轨上方"
)
```

**过滤条件**：
- ❌ 既不满足连续两根阳线
- ❌ 也不满足开盘在布林中轨上方

---

#### 1.2 技术指标过滤
```python
LOGGER.info(
    "signal.e1.technical_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    ao_up=ao_up,
    ao_value="xxx",
    cci_positive=cci_positive,
    cci14="xxx",
    cci6="xxx",
    obv_rising=obv_rising,
    obv="xxx",
    obv_ema20="xxx",
    tech_score="x/3",
    reason="技术指标不足：需满足2/3条件，实际x/3"
)
```

**过滤条件**：
- AO > 0 且上升
- CCI14 > 0 且 CCI6 > 0
- OBV >= OBV_EMA20
- ❌ **至少需满足2个条件**

---

#### 1.3 MFI/Stoch过滤

**1.3.1 数据缺失**
```python
LOGGER.warning(
    "signal.e1.mfi_data_missing",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    current_mfi=str(current_mfi),
    lag5_mfi=str(lag5_mfi),
    reason="MFI数据缺失，拒绝信号"
)
```

**1.3.2 条件不满足**
```python
LOGGER.info(
    "signal.e1.mfi_stoch_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    mfi_pass=mfi_pass,
    mfi_current="xx.xx",
    mfi_lag5="xx.xx",
    mfi_slope="xx.xx",
    mfi_check="MFI>50: True/False, 斜率>0: True/False",
    stoch_pass=stoch_pass,
    stoch_k_prev2="xx.xx",
    stoch_d_prev2="xx.xx",
    stoch_k_prev="xx.xx",
    stoch_d_prev="xx.xx",
    stoch_check="金叉: True/False, K<80: True/False",
    reason="MFI/Stoch过滤：MFI和Stoch条件均不满足"
)
```

**MFI条件**：
- MFI14 > 50
- MFI斜率 > 0（相对5根K线前）

**Stoch条件**：
- Stoch金叉（K上穿D）
- Stoch_K < 80（避免超买）

**过滤逻辑**：❌ MFI和Stoch **至少满足一个**

---

#### 1.4 成功生成信号
```python
LOGGER.info(
    "signal.e1.buy_signal_generated",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    signal_code="SIG_OPEN_CHASE_BUY",
    candle_pattern="two_green/opens_above_mid",
    tech_indicators="AO:True, CCI:True, OBV:False",
    mfi_stoch="MFI:True, Stoch:False / disabled",
    reason="所有过滤条件已通过，生成开盘追涨买入信号"
)
```

---

### 2. **E2信号（SIG_REBOUND_BUY）- 反弹买入**

#### 2.1 技术指标过滤
```python
LOGGER.info(
    "signal.e2.technical_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    rvol_pass=rvol_pass,
    rvol6="x.xx",
    obv_cross=obv_cross,
    obv_prev="xxx",
    obv_current="xxx",
    ao_positive=ao_positive,
    ao_prev="x.xxxx",
    ao_current="x.xxxx",
    tech_score="x/3",
    reason="E2技术指标不足：需满足2/3条件，实际x/3"
)
```

**过滤条件**：
- RVOL6 >= 2（量能放大）
- OBV上穿OBV_EMA20
- AO由负转正
- ❌ **至少需满足2个条件**

---

#### 2.2 情景过滤
```python
LOGGER.info(
    "signal.e2.scenario_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    scenario_a=scenario_a,
    scenario_b=scenario_b,
    time_window="hh:mm",
    rsi6_prev="xx.xx",
    rsi6_current="xx.xx",
    reason="E2情景不符：既不是RSI30金叉（9:33前），也不是二次探底反弹（10:00-10:30）"
)
```

**情景A**（9:33前）：RSI6从<30上穿30  
**情景B**（10:00-10:30）：二次探底反弹

---

#### 2.3 MFI/Stoch过滤

**数据缺失**
```python
LOGGER.warning(
    "signal.e2.mfi_data_missing",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    current_mfi=str(current_mfi),
    lag4_mfi=str(lag4_mfi),
    reason="E2 MFI数据缺失，拒绝信号"
)
```

**条件不满足**
```python
LOGGER.info(
    "signal.e2.mfi_stoch_filtered",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    mfi_pass=mfi_pass,
    mfi_current="xx.xx",
    mfi_lag4="xx.xx",
    mfi_slope="xx.xx",
    stoch_pass=stoch_pass,
    stoch_k_prev="xx.xx",
    stoch_d_prev="xx.xx",
    stoch_k_current="xx.xx",
    stoch_d_current="xx.xx",
    reason="E2 MFI/Stoch过滤：MFI和Stoch条件均不满足"
)
```

---

#### 2.4 成功生成信号
```python
LOGGER.info(
    "signal.e2.buy_signal_generated",
    symbol=event.symbol,
    timestamp=et_time.isoformat(),
    signal_code="SIG_REBOUND_BUY",
    scenario="RSI30金叉 / 二次探底反弹(+x.xx%)",
    tech_indicators="RVOL:True, OBV_Cross:False, AO+:True",
    mfi_stoch="MFI:True, Stoch:False / disabled",
    reason="所有E2过滤条件已通过，生成反弹买入信号"
)
```

---

## 🔧 代码改进

### 1. **MFI数据空值检查**

在计算MFI斜率前，增加了空值检查，避免Decimal异常：

```python
# 检查MFI数据有效性
current_mfi = prev["mfi14"]
lag5_mfi = df.iloc[-5]["mfi14"]

if pd.isna(current_mfi) or pd.isna(lag5_mfi):
    LOGGER.warning(...)
    return None

mfi_slope = current_mfi - lag5_mfi
```

**修复的BUG**：
- ✅ 避免了HUBS股票的`decimal.InvalidOperation`异常
- ✅ 对于指标数据缺失的股票，明确拒绝并记录日志

---

### 2. **结构化日志格式**

所有日志使用统一的结构化格式：

```python
LOGGER.info(
    "signal.e1.xxx_filtered",  # 事件名称
    symbol=event.symbol,        # 股票代码
    timestamp=et_time.isoformat(),  # 时间戳
    # ... 详细参数 ...
    reason="具体原因的中文描述"
)
```

**优势**：
- 易于解析和分析
- 支持日志聚合和查询
- 方便生成诊断报告

---

## 📊 使用示例

### 查询被MFI/Stoch过滤的股票

```bash
grep "signal.e1.mfi_stoch_filtered" backtest_run.log | jq '{symbol, mfi_check, stoch_check}'
```

### 查询K线形态被过滤的股票

```bash
grep "signal.e1.candle_pattern_filtered" backtest_run.log | jq '{symbol, two_green, opens_above_mid}'
```

### 查询成功生成的买入信号

```bash
grep "buy_signal_generated" backtest_run.log | jq '{symbol, signal_code, timestamp}'
```

---

## 🎯 已有日志（保留）

以下日志在原代码中已存在，本次改进予以保留：

1. **VIX门槛过滤**：`record_signal_filtered("VIX_GATE")`
2. **Top5过滤**：`record_signal_filtered("TOP5_FILTER")`
3. **盘前涨幅过滤**：
   - `record_signal_filtered("PREMARKET_GAP_MISSING")`
   - `record_signal_filtered("PREMARKET_GAP_FILTER")`
4. **时间窗口过滤**：`record_signal_filtered("TIME_WINDOW")`
5. **MA60过滤**：`record_signal_filtered("MA60_FILTER")`

---

## 📈 效果预期

启用详细日志后，可以清楚地看到：

1. **9月2-5日回测案例**：
   - 为什么前3天Top5=0？→ v_daily_ma60视图缺失
   - 为什么9月5日有10只Top5但无买入信号？
     - AVGO：K线形态过滤（9:30是红线）
     - U：K线形态过滤（9:29是红线）
     - TSM：**所有条件都通过**，需查看仓位检查日志
     - AFRM：MFI < 50
     - ANET：MFI斜率为负
     - ASML：MFI斜率为负
     - TSLA：MFI斜率为负
     - HUBS：MFI数据缺失（Decimal异常）

2. **实盘监控**：
   - 实时了解哪些Top10股票被过滤
   - 评估过滤条件的严格程度
   - 优化参数配置

---

## 🚀 下一步

1. ✅ **已完成**：E1和E2信号的过滤日志
2. 🔄 **待添加**：PM底部信号的过滤日志（如果需要）
3. 🔄 **待添加**：`_can_open_position`函数的拒绝日志
4. 📊 **建议**：创建日志分析脚本，自动生成过滤统计报告

---

*文档生成时间：2025-10-30*  
*版本：v1.0*

