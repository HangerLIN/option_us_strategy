# 指标缺失问题分析与修复方案

**问题日期:** 2025-10-21  
**影响范围:** 回测数据完整性  
**严重程度:** 中等（导致574处指标缺失）

---

## 一、问题总结

### 1.1 表现
回测报告显示**574处指标缺失**，分布如下：
- BOLL: 137处
- ATR: 57处  
- AO: 301处（最多）
- CCI: 95处
- RSI/OBV: 0处（完整）

### 1.2 影响
- 指标缺失率约2.5% (574/23,113)
- 主要影响信号生成准确性
- RVOL6指标完全缺失（最严重）

---

## 二、根本原因分析

### 2.1 直接原因
**`rvol_baseline_eq`表完全为空**

查询结果：
```sql
SELECT COUNT(*) FROM rvol_baseline_eq;
-- 结果: 0 行
```

**影响链路：**
```
rvol_baseline_eq表为空
    ↓
baseline_map = {} (空字典)
    ↓
compute_indicators(..., baseline_map={})
    ↓
rvol6 = NaN (无法计算)
    ↓
_scan_equity_gaps检测到指标缺失
    ↓
写入risk_events: DATA_GAP_DETECTED
```

### 2.2 间接原因
**IBKR补数流程未成功运行**

在之前的回测中：
1. 回测代码检测到数据缺口
2. 尝试连接IBKR进行补数
3. **连接失败**：`client id 88 already in use`
4. 补数流程中断，数据库缺口保留
5. 指标无法补全

### 2.3 其他缺失原因
**热身期（Warmup Period）缺失**

部分指标需要历史数据计算：
- BOLL(20): 需要前20根K线
- ATR(14): 需要前14根K线  
- AO(5,34): 需要前34根K线（因此缺失最多）
- CCI(14): 需要前14根K线

**结论:** 这部分缺失属于**正常现象**，每个交易日的前几根K线会自然缺失

---

## 三、区分正常缺失与异常缺失

### 3.1 正常缺失（热身期）
**特征：**
- 仅出现在每个交易日的开盘前几分钟
- 缺失数量 = 指标窗口大小（20/14/34根）
- 分布均匀，每天相同

**示例：**
```
2025-10-01 09:30 - 09:49 (前20分钟) BOLL缺失 ✓ 正常
2025-10-01 09:30 - 10:03 (前34分钟) AO缺失  ✓ 正常
```

### 3.2 异常缺失（RVOL6）
**特征：**
- **全天所有时段**都缺失
- 原因：`rvol_baseline_eq`表为空
- 影响：所有rvol6指标 = NaN

**验证：**
```sql
SELECT COUNT(*) 
FROM indicators_eq_1m 
WHERE rvol6 IS NULL 
  AND ts_end >= '2025-10-01 13:30:00+00'
  AND ts_end < '2025-10-08 13:30:00+00';
-- 预计结果: 数千行
```

---

## 四、修复方案

### 方案A: 刷新RVOL基线 + 重新回测（推荐）

#### 步骤1: 确保IBKR连接可用
```bash
# 检查是否有进程占用ClientId
ps aux | grep -E "python|tws|ibgw" | grep -v grep

# 如果有，杀掉旧进程
kill -9 <PID>

# 确认.env配置
cat .env | grep IB_CLIENT_ID
# 应该输出: IB_CLIENT_ID=88
```

#### 步骤2: 运行RVOL基线刷新脚本
```bash
cd /Users/hangerlin/Desktop/option_us_strategy
source .venv/bin/activate

# 刷新rvol_baseline_eq表
PYTHONPATH=$PWD python refresh_rvol_baseline.py
```

**预期输出：**
```
{"event": "rvol_baseline.start", "symbols": ["AAPL", "AMD", ...], "lookback_days": 40}
{"event": "rvol_baseline.processing", "symbol": "AAPL"}
{"event": "rvol_baseline.bars_fetched", "symbol": "AAPL", "count": 7800}
{"event": "rvol_baseline.completed", "symbol": "AAPL", "rows_inserted": 390}
...
{"event": "rvol_baseline.all_completed", "symbols": 13}
```

#### 步骤3: 验证基线数据
```bash
python << 'PYEOF'
from sqlalchemy import create_engine, text
from libs.core import get_settings

settings = get_settings()
engine = create_engine(settings.database_url)

with engine.connect() as conn:
    result = conn.execute(text("""
        SELECT 
            symbol,
            COUNT(*) as rows,
            MIN(minute_index) as min_idx,
            MAX(minute_index) as max_idx
        FROM rvol_baseline_eq
        GROUP BY symbol
        ORDER BY symbol
    """))
    
    for row in result:
        print(f"{row[0]:6s}: {row[1]:3d} rows, idx {row[2]}-{row[3]}")
PYEOF
```

**预期输出：**
```
AAPL  : 390 rows, idx 1-390
AMD   : 390 rows, idx 1-390
...
```

#### 步骤4: 重新运行回测
```bash
python apps/backtest/main.py \
  --start 2025-10-01T09:00:00-04:00 \
  --end 2025-10-08T17:00:00-04:00 \
  --signal-mode recompute \
  --track equity
```

**预期结果：**
- RVOL6指标完全填充
- 指标缺失数从574降至<100（仅热身期）
- 缺失率从2.5%降至<0.5%

---

### 方案B: 禁用RVOL过滤（权宜之计）

如果暂时无法刷新RVOL基线，可以临时禁用依赖RVOL的信号过滤。

**修改位置：**
```python
# apps/signal_svc/engine.py
def _detect_e1(...):
    # 注释掉RVOL过滤
    # if event.rvol6 < 1.5:
    #     return None
    ...
```

**缺点：**
- 信号质量可能下降
- 违背文档规范
- 不建议用于生产

---

## 五、预防措施

### 5.1 回测前检查清单
```bash
# 1. 检查RVOL基线
echo "SELECT COUNT(DISTINCT symbol) FROM rvol_baseline_eq;" | psql $DATABASE_URL

# 2. 检查K线数据
echo "SELECT COUNT(*) FROM bars1m_equity WHERE ts_end >= '2025-10-01';" | psql $DATABASE_URL

# 3. 检查指标数据
echo "SELECT COUNT(*) FROM indicators_eq_1m WHERE ts_end >= '2025-10-01';" | psql $DATABASE_URL

# 4. 测试IBKR连接
python -c "from libs.infra import build_ibkr_client; from libs.core import get_settings; \
           ib = build_ibkr_client(get_settings()); \
           print('Connected:', ib.is_connected()); \
           ib.disconnect_and_stop()"
```

### 5.2 自动化RVOL基线刷新

**添加到nightly cron任务：**
```bash
# 每天16:30 ET刷新RVOL基线（盘后）
30 16 * * 1-5 cd /path/to/option_us_strategy && \
  source .venv/bin/activate && \
  PYTHONPATH=$PWD python refresh_rvol_baseline.py >> logs/rvol_refresh.log 2>&1
```

### 5.3 监控指标
```sql
-- 创建监控视图
CREATE OR REPLACE VIEW v_indicator_health AS
SELECT 
    DATE(ts_end AT TIME ZONE 'America/New_York') as trade_date,
    symbol,
    COUNT(*) as total_rows,
    COUNT(rsi6) as rsi6_present,
    COUNT(rvol6) as rvol6_present,
    COUNT(boll_mid) as boll_present,
    COUNT(*) - COUNT(rvol6) as rvol6_missing
FROM indicators_eq_1m
WHERE ts_end >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY trade_date, symbol
HAVING COUNT(*) - COUNT(rvol6) > 0  -- 只显示有缺失的
ORDER BY trade_date DESC, rvol6_missing DESC;
```

---

## 六、常见问题

### Q1: 为什么不直接用IBKR补数？
**A:** IBKR补数需要：
1. 稳定的网络连接
2. 可用的ClientId
3. 实时数据订阅限制
4. API限流控制

RVOL基线是一次性计算，更稳定可靠。

### Q2: 热身期缺失会影响回测吗？
**A:** 影响很小：
- 每天前34分钟（<10%交易时间）
- 通常没有重要信号
- 开盘追高信号（E1）在09:31，已过热身期

### Q3: 如果IBKR连接一直失败怎么办？
**A:** 两个选择：
1. 使用CSV/Parquet历史数据导入（需修改数据管道）
2. 从其他数据源获取RVOL基线（如Polygon.io）

### Q4: 574处缺失是否影响本次回测结果？
**A:** **影响较小**，因为：
- 主要是热身期的BOLL/ATR/AO（正常）
- RVOL6虽然全缺，但回测仍然跑通
- 信号仍然生成（可能精度略降）

但建议**修复后重跑**以确保数据质量。

---

## 七、执行计划

### 立即执行（今天）
1. ✅ 等待当前回测完成
2. ⏳ 运行`refresh_rvol_baseline.py`
3. ⏳ 验证基线数据
4. ⏳ 重新运行回测
5. ⏳ 生成新的报告对比

### 短期（本周）
1. 将RVOL刷新加入每日任务
2. 添加数据健康监控
3. 更新文档

### 中期（本月）
1. 优化IBKR补数逻辑
2. 增加更多数据源冗余
3. 建立自动化测试

---

## 八、技术细节

### 8.1 RVOL计算公式
```
RVOL(6) = EMA6(current_volume / baseline_volume[minute_index])

baseline_volume[minute_index] = AVG(volume_at_same_minute, last_20_trading_days)
```

### 8.2 数据表结构
```sql
CREATE TABLE rvol_baseline_eq (
    symbol        text      NOT NULL,
    minute_index  smallint  NOT NULL CHECK (minute_index BETWEEN 1 AND 390),
    mean_vol_20d  double precision NOT NULL,
    updated_at    timestamptz DEFAULT now(),
    PRIMARY KEY (symbol, minute_index)
);
```

### 8.3 刷新逻辑流程
```
1. 连接IBKR
2. 对每个symbol:
   a. 获取过去40天的1分钟历史数据
   b. 按trade_date和minute_index分组
   c. 计算每个minute_index的均量（至少5天数据）
   d. Upsert到rvol_baseline_eq
3. 断开IBKR
```

---

**报告生成时间:** 2025-10-21  
**脚本路径:** `refresh_rvol_baseline.py`  
**预计修复时间:** 30-60分钟（取决于IBKR连接速度）

**下一步:** 等待当前回测完成 → 运行刷新脚本 → 重新回测 → 对比结果

